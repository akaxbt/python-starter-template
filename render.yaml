version: "1"

services:
  - type: worker
    name: python-starter-template
    runtime: docker
    repo: https://github.com/akaxbt/python-starter-template
    plan: starter
    envVars: []
    region: singapore
    dockerContext: .
    dockerfilePath: ./Containerfile
